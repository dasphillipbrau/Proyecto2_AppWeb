@model IEnumerable<Proyecto2_AppWeb.Models.BadProspect>

@{
    ViewBag.Title = "El Polo Norte";
    ViewBag.Message = "Clientes Deficientes";

}

<h1 class="store-title">@ViewBag.Title</h1>
<h2 class="page-title">@ViewBag.Message</h2>

@using (Html.BeginForm())
{
    if (TempData["ResultFound"] != null)
    {
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Cédula</th>
                    <th scope="col">Tipo de Cliente</th>
                    <th scope="col">Nombre Completo</th>
                    <th scope="col">Número Telefónico</th>
                    <th scope="col">Monto total en el año anterior</th>
                    <th scope="col">Monto total en el mes anterior</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var client in Model)
                {
                <tr>
                    <th scope="row">@client.Id</th>
                    <td>@client.ClientType</td>
                    <td>@client.FullName</td>
                    <td>@client.PhoneNumber</td>
                    <td>@client.LastYearPurchases</td>
                    <td>@client.RecentPurchasesAmount</td>
                </tr>
                }
            </tbody>
        </table>

    }
    else
    {
        if (Session["ResultFound"] == null)
        {
            <p class="text-success">No hay clientes deficientes</p>
        }
    }
}
